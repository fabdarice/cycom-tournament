<% form_for(@tree) do |f| %>
  <%= render 'shared/error_messages', :target => @tree %>

  <%= f.hidden_field :tournament_id %>
  <%= f.select :tree_type, Tree::TYPES %>
  <%= f.text_field :best_of %>
  <%= f.submit @tree.new_record? ? t("validate.new") : t("validate.edit") %>
<% end %>